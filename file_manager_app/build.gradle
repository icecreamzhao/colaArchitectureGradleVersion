/**
 * file_manager的 app 层, 主要负责获取输入，
 * 组装上下文，参数校验，调用领域层做业务处理，
 * 如果需要的话，发送消息通知等。层次是开放的，
 * 应用层也可以绕过领域层(Domain)，直接访问基础实施层；
 */

archivesBaseName = 'file_manager_app'

dependencies {
    api project(':file_manager_client')
    api project(':file_manager_infrastructure')
}